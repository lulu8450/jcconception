<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta content="width=device-width, initial-scale=1.0" name="viewport">

        <title>{% block title %}{% endblock %}</title>

        <!-- Favicons -->
        <link href="{{ asset('img/mixlogo.png') }}" rel="icon">
        <link href="{{ asset('img/mixlogo.png') }}" rel="apple-touch-icon">

        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body class="{% block body_class %}{% endblock %}">
    <!-- ======= Header ======= -->
    <header id="header" class="fixed-top d-flex align-items-center">
        <div class="container d-flex justify-content-between">
        <div class="logo">
            <a href="{{ path('app_home') }}"><img src="{{ asset('img/mixlogo.png') }}" alt="" class="img-fluid"> JC Conception</a>
        </div>
        <nav id="navbar" class="navbar">
            <ul>
                <li><a class="nav-link scrollto active" href="#hero">Accueil</a></li>
                <li><a class="nav-link scrollto" href="#about">A propos</a></li>
                <li><a class="nav-link scrollto" href="#services">Services</a></li>
                <li><a class="nav-link scrollto" href="#contact">Contact</a></li>
            </ul>
            <i class="bi bi-list mobile-nav-toggle"></i>
            <div class="d-lg-flex col-lg-3 justify-content-lg-end">
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    
                    <div class="mb-3 text-center text-white">
                        <h3>{{ app.user.userIdentifier }}</h3>
                            <a href="{{ path('app_logout') }}" class="btn btn-danger">Se déconnecter</a>
                    </div>
                {% else %}
                    <a href="/register" class="btn btn-success">S'enregistrer</a>
                    <a href="/login" class="btn btn-primary">Se connecter</a>
                {% endif %}
			</div>
        </nav>
        </div>
    </header>
    <!-- End Header -->
        {% block body %}{% endblock %}
    </body>
</html>